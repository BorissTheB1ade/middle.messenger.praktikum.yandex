<div class="user-dropdown-content">
    <h3>{{title}}</h3>
    <div class="user-list">
        {{#if hasUsers}}
            {{{usersHTML}}}
        {{else}}
            <div class="no-users">Нет пользователей</div>
        {{/if}}
    </div>
</div>
